# Soil Engineering Properties Prediction Using ANN

## Strict Input Range Limitation

During manual prediction, every input field is automatically restricted to the minimum and maximum values observed in the training dataset. Users cannot enter values outside these ranges.

Example: if LL in training data ranges from 30 to 80, the user can only enter LL values from 30 to 80.
